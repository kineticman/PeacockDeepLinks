# Peacock TV Scraper - Environment Configuration

# =============================================================================
# Server Configuration
# =============================================================================

# Server host (IP address or hostname) - REQUIRED for ADBTuner
# Just the IP/hostname, NO http:// and NO port
# Examples:
#   PEACOCK_SERVER_HOST=192.168.86.72
#   PEACOCK_SERVER_HOST=10.0.0.50
#   PEACOCK_SERVER_HOST=peacock.local
PEACOCK_SERVER_HOST=192.168.86.72

# Web server port (default: 6655)
PEACOCK_PORT=6655

# =============================================================================
# Scraper Configuration
# =============================================================================

# Number of virtual channels/lanes to create (default: 10)
PEACOCK_LANES=10

# How many days ahead to scrape events (default: 7)
PEACOCK_DAYS_AHEAD=7

# Event end padding in minutes (default: 45)
# Adds buffer after events to account for overtime
PEACOCK_PADDING_MINUTES=45

# Starting channel number for lanes (default: 9000)
# Lanes will be numbered: 9000, 9001, 9002, etc.
PEACOCK_LANE_START_CH=9000

# Placeholder block duration in minutes (default: 60)
PEACOCK_PLACEHOLDER_BLOCK_MINUTES=60

# =============================================================================
# API Configuration
# =============================================================================

# Peacock API endpoint slug (default: /sports/live-and-upcoming)
PEACOCK_SLUG=/sports/live-and-upcoming

# =============================================================================
# Refresh Schedule
# =============================================================================

# Cron expression for automatic refresh (default: 15 3 * * *)
# Format: minute hour day month day_of_week
# Examples:
#   15 3 * * *      - 3:15 AM daily (default)
#   0 */4 * * *     - Every 4 hours
#   30 1,13 * * *   - 1:30 AM and 1:30 PM daily
#   0 6 * * 0       - 6:00 AM every Sunday
PEACOCK_REFRESH_CRON=15 3 * * *

# =============================================================================
# Advanced Configuration (usually don't need to change)
# =============================================================================

# Alternatively, you can set the full server URL (overrides HOST+PORT)
# PEACOCK_SERVER_URL=http://192.168.86.72:6655

# Database path (inside container)
# PEACOCK_DB_PATH=/data/peacock_events.db

# XMLTV output paths (inside container)
# PEACOCK_LANES_XML_PATH=/data/peacock_lanes.xml
# PEACOCK_DIRECT_XML_PATH=/data/peacock_direct.xml

# M3U output paths (inside container)
# PEACOCK_LANES_M3U_PATH=/data/peacock_lanes.m3u
# PEACOCK_DIRECT_M3U_PATH=/data/peacock_direct.m3u
